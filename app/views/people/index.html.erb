<% if @error %>
 <h1>Error: <%= @error %></h1>
<% else %>
  <h1>Results for '<%= @term %>':</h1>

  <ul>
    <% @resources["results"].each do |result| %>
      <li>
        <%= link_to full_name(result), person_path(result["tiss_id"]) %>
      </li>
    <% end %>
  </ul>
<% end %>
